
const styles = theme => ({
  // Selects any children of the container, so each card/paper
  root: {
    flexGrow: 1,
  },
  headerTextStyle: {
    marginTop: 0,
    marginBottom: 0,
  },
  dsButtons: {
    paddingTop: 10,
    paddingBottom: 20,
  },
  uploadButton: {
    marginRight: 15
  }
});

export default styles;